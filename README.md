# Netflix Content Analysis with RAG

This project provides a full-stack skeleton for a Retrieval-Augmented Generation (RAG) application focused on Netflix content analytics. It includes a FastAPI backend with placeholder embedding and retrieval logic, PostgreSQL persistence for metadata and embeddings, and a React interface for submitting natural-language queries and viewing generated responses based on retrieved context.

Work in progress
